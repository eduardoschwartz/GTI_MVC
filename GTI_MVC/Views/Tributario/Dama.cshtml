@model GTI_Mvc.ViewModels.CertidaoViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Emissão de DAM";
}

<style>
    .textBoxBlue {
        height: 25px;
        position: relative;
        border: 1px solid #0094ff;
        border-color: rgba(0, 0, 0, .14);
        background-color: white;
        font-size: 14px;
        box-shadow: 0 1px 1px 0 rgba(0,0,0,0.24),0 1px 6px 0 rgba(0,0,0,0.19);
    }
</style>

<label style="color: brown;font-size: 16px;font-style: italic;font-weight: normal;text-decoration:underline" ;>Consulta e atualização de débitos vencidos</label><br />
<br />
<br />

@using (Html.BeginForm("Dama", "Tributario", FormMethod.Post, new { @id = "MainForm" })) {

    <table width="500px">
        <tr >
            <td style="padding-top:7px; width:150px">@Html.LabelFor(m => m.DataVencimento, new { @style = "font-weight:normal" })</td>
            <td style="width:300px">@Html.TextBoxFor(m => m.DataVencimento, new { @id = "vencto", @class = "textBoxBlue", @style = "max-width:100px", @onKeyPress = "return formata(this, '§§/§§/§§§§', event)" })</td>
        </tr>
    </table>
    <br />
    <p style="color:red;">
        Conforme Decreto Municipal Nº 7.162, de 30 de abril de 2020, Art. 1º fica prorrogado o pagamento de juros e multa <br />
        decorrente do pagamento extemporâneo de todos os tributos municipais devidos pelo pagamento em atraso das parcelas <br />
        dos meses de abril, maio e junho, cujo pagamento deverá ser realizado dentro do corrente exercício, em data a ser<br />
        definida.
    </p>
    <br />
    <div class="col-sm-10">
        &nbsp;&nbsp;<button type="submit" class="green_button"> Consultar</button>
    </div>
    <br />
   
    <br />
    <div class="form-group row">
        <label style="font-weight:normal;color:red" id="errorLabel">@ViewBag.Result</label>
    </div>
    <br />

}

@Html.ActionLink("Retornar ao menu principal", "Login_gti", "Home")

@section scripts{

    <script type="text/javascript">

        $(document).ready(function () {
            $("#MainForm").submit(function () {
                return ValidateForm();
            });
        });

        function ValidateForm() {
            ClearError();
            var _data = document.getElementById('vencto').value;

            if (_data.length < 10) {
                document.getElementById('errorLabel').innerHTML = "Data de vencimento inválida";
                return false;
            }

            return true;
        }

        function ClearError() {
            document.getElementById('errorLabel').innerHTML = "";
        }

    </script>
}
